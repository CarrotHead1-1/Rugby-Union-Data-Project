# Databricks Asset Bundle configuration, more info here: https://docs.databricks.com/en/dev-tools/bundles/settings.html
bundle:
  name: "rugby_data_project"

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  # The 'dev' target, for development purposes. This target is the default.
  dev:
    mode: development
    default: true
    workspace:
      host: https://dbc-423a5d50-03a6.cloud.databricks.com

  # The 'prod' target, used for production deployment.
  prod:
    mode: production
    workspace:
      host: https://dbc-423a5d50-03a6.cloud.databricks.com
      # We always use /Users/escott.kieron@gmail.com for all resources to make sure we only have a single copy.
      root_path: /Users/escott.kieron@gmail.com/.bundle/${bundle.name}/${bundle.target}
    run_as:
      # This runs as escott.kieron@gmail.com in production. We could also use a service principal here,
      # see https://docs.databricks.com/dev-tools/bundles/permissions.html.
      user_name: escott.kieron@gmail.com
