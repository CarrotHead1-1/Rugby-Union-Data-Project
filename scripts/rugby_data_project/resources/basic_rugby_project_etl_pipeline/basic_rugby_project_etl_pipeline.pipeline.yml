resources:
  pipelines:
    basic_rugby_project_etl_pipeline:
      name: basic_rugby_project_etl_pipeline
      libraries:
        - glob:
            include: transformations/**
      serverless: true
      catalog: workspace
      schema: ${workspace.current_user.short_name}
      root_path: .
